<div class="title">
  <h1><%= @contact.name %></h1>
</div>
<div class="inner">
  <p id="notice"><%= notice %></p>

  <div class="add_order">
    <h2>Add a new order</h2>
    <%= render 'orders/form' %>
  </div>

  <% unless @contact.orders.empty? %>
    <h2>Orders:</h2>
    <ul>
      <%= render @contact.orders.order('created_at DESC') %>
    </ul>
  <% end %>
</div>

<% content_for :side do %>
  <div class="inner">
    <%= link_to 'Edit', edit_contact_path(@contact) %>
  </div>
<% end %>
