<div class="field">
  <%= f.collection_select :product_id, Product.all, :id, :name, :prompt => t('.select_a_product') %>
  &times;
  <%= f.text_field :quantity, :size => "2" %>
  <%= f.link_to_remove t('.remove') %>
</div>
