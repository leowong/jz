<% title t('.editing_order'), false %>

<% content_for :header do %>
  <div class="buttons">
    <ul>
      <li><%= link_to t('.show'), @order %></li>
      <li><%= link_to t('.back'), orders_path %></li>
    </ul>
  </div>
  <h1><%= t('.editing_order') %></h1>
<% end %>

<div class="inner">
  <%= render 'form' %>
</div>
